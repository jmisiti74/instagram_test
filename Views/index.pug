doctype html
html
  head
    title= `${title}`
  body
    form(method='POST' action='/')
      div
        label(for='username') Username:
        input#username(type='text', placeholder='sandbox username' name='username')
      br
      button(type='submit') Get likes per post
